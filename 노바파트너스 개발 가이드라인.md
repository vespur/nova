# 노바파트너스 개발 가이드라인

**버전**: 1.0  
**최종 수정**: 2025-01-24  
**프로젝트**: 식스샵 프로 웹빌더 템플릿

---

## 📋 목차

1. [프로젝트 개요](#1-프로젝트-개요)
2. [디자인 시스템 2026](#2-디자인-시스템-2026)
3. [사이트 구조](#3-사이트-구조)
4. [레이아웃 시스템](#4-레이아웃-시스템)
5. [커스텀 블록 개발 원칙](#5-커스텀-블록-개발-원칙)
6. [설정 패널 구성](#6-설정-패널-구성)
7. [마우스 인터랙션](#7-마우스-인터랙션)
8. [페이지별 개발 가이드](#8-페이지별-개발-가이드)
9. [품질 기준](#9-품질-기준)
10. [부록](#10-부록)

---

## 1. 프로젝트 개요

### 1.1 프로젝트 정보

**프로젝트명**: 노바파트너스 웹사이트 템플릿  
**유형**: 엔터프라이즈급 AI 솔루션 기업 웹사이트  
**총 페이지 수**: 23개  
**커스텀 블록 목표**: 15개 (기본 5개 + 추가 10개)

---

### 1.2 타겟 사용자

- **1차**: 식스샵 템플릿 구매자 (비개발자)
- **2차**: 엔터프라이즈 B2B 기업
- **3차**: AI/IT/컨설팅 업종 기업

---

### 1.3 프로젝트 목표

✅ **지원금 200만원 달성**
- 커스텀 블록 10개 이상
- 신규 제작 블록 5개 이상
- 고품질 설정 패널

✅ **차별화 포인트**
- 2026 최신 디자인 트렌드 반영
- 마우스 인터랙션 풍부
- 완전한 커스터마이징 가능

---

## 2. 디자인 시스템 2026

### 2.1 색상 팔레트

```css
:root {
  /* Primary Colors */
  --primary-500: #0055FF;
  --primary-600: #0044CC;
  --primary-700: #003399;
  
  /* Gray Scale */
  --gray-50: #F8F9FA;
  --gray-100: #E9ECEF;
  --gray-200: #DEE2E6;
  --gray-500: #6C757D;
  --gray-700: #495057;
  --gray-800: #343A40;
  --gray-900: #212529;
  
  /* Semantic */
  --white: #FFFFFF;
  --black: #000000;
  --success: #28A745;
  --error: #DC3545;
}
```

**사용 가이드**:
- Primary: 버튼, 링크, 강조 요소
- Gray: 텍스트, 배경, 테두리
- White/Black: 배경, 텍스트

---

### 2.2 타이포그래피

```css
:root {
  /* Font Family */
  --font-primary: 'Pretendard Variable', -apple-system, sans-serif;
  
  /* Font Sizes */
  --text-xs: 12px;
  --text-sm: 14px;
  --text-base: 16px;
  --text-lg: 18px;
  --text-xl: 20px;
  --text-2xl: 24px;
  --text-3xl: 28px;
  --text-4xl: 32px;
  --text-5xl: 40px;
  --text-6xl: 48px;
  
  /* Line Heights */
  --leading-tight: 1.25;
  --leading-normal: 1.5;
  --leading-relaxed: 1.625;
}
```

**권장 조합**:
- **제목**: 40-48px / 700 / -0.5px letter-spacing
- **부제목**: 18-20px / 400 / 1.5 line-height
- **본문**: 16px / 400 / 1.5 line-height
- **작은 텍스트**: 14px / 400 / 1.625 line-height

---

### 2.3 간격 (Spacing)

```css
:root {
  /* Spacing Scale */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-5: 24px;
  --space-6: 32px;
  --space-8: 48px;
  --space-10: 64px;
  --space-12: 80px;
  --space-15: 120px;
  --space-20: 160px;
}
```

**권장 사용**:
- **섹션 여백**: 80-120px (데스크톱), 60-80px (모바일)
- **요소 간격**: 32-48px
- **카드 내부**: 40px
- **텍스트 여백**: 16-24px

---

### 2.4 Border Radius (둥근 모서리)

```css
:root {
  --radius-sm: 8px;   /* 버튼, 작은 카드 */
  --radius-md: 12px;  /* 기본 카드 */
  --radius-lg: 16px;  /* 큰 카드 */
  --radius-xl: 24px;  /* 대형 카드, 이미지 */
  --radius-2xl: 32px; /* Hero 섹션 */
  --radius-full: 9999px; /* 원형 */
}
```

**2026 트렌드**: 크게 둥글게 (12px 이상)

---

### 2.5 그림자 (Shadow)

```css
:root {
  /* Standard Shadows */
  --shadow-sm: 0 2px 8px rgba(0, 0, 0, 0.08);
  --shadow-md: 0 4px 16px rgba(0, 0, 0, 0.12);
  --shadow-lg: 0 8px 32px rgba(0, 0, 0, 0.16);
  --shadow-xl: 0 12px 48px rgba(0, 0, 0, 0.20);
  
  /* Primary Glow (2026 트렌드) */
  --shadow-primary: 0 4px 16px rgba(0, 85, 255, 0.25);
  --shadow-primary-lg: 0 8px 24px rgba(0, 85, 255, 0.35);
}
```

**사용 가이드**:
- 기본 카드: `shadow-sm`
- Hover 카드: `shadow-lg`
- Primary 버튼: `shadow-primary`

---

### 2.6 애니메이션

```css
:root {
  /* Duration */
  --duration-fast: 0.2s;
  --duration-normal: 0.3s;
  --duration-slow: 0.6s;
  
  /* Easing */
  --ease: cubic-bezier(0.4, 0, 0.2, 1);
  --ease-in: cubic-bezier(0.4, 0, 1, 1);
  --ease-out: cubic-bezier(0, 0, 0.2, 1);
}
```

**권장 사용**:
- **Hover 효과**: 0.2-0.3s
- **스크롤 애니메이션**: 0.6-0.8s
- **Modal/팝업**: 0.3s

---

### 2.7 특수 효과 (2026 트렌드)

#### Glassmorphism
```css
.glass-effect {
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}
```

#### Gradient Text
```css
.gradient-text {
  background: linear-gradient(135deg, #0055FF 0%, #0099FF 100%);
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;
  background-clip: text;
}
```

#### Glow Effects
```css
.glow-effect {
  box-shadow: 0 0 20px rgba(0, 85, 255, 0.3);
  filter: drop-shadow(0 0 10px rgba(0, 85, 255, 0.5));
}
```

#### Hover Transform
```css
.hover-lift {
  transition: transform 0.3s ease;
}

.hover-lift:hover {
  transform: translateY(-8px);
}
```

---

## 3. 사이트 구조

### 3.1 전체 페이지 (23개)

```
/ (HOME) - 1개

/solutions/ - 9개
├─ /solutions/ai-platform/
├─ /solutions/ai-platform/features
├─ /solutions/ai-platform/pricing
├─ /solutions/ai-platform/case-studies
├─ /solutions/ai-platform/demo
├─ /solutions/industries/finance
├─ /solutions/industries/manufacturing
├─ /solutions/industries/healthcare
└─ /solutions/industries/retail

/value/ - 2개
├─ /value/
└─ /value/security-compliance

/support/ - 4개
├─ /support/technical
├─ /support/downloads
├─ /support/faq
└─ /support/news

/company/ - 6개
├─ /company/
├─ /company/ceo
├─ /company/history
├─ /company/ethics
├─ /company/careers
└─ /company/locations

/contact/ - 1개
```

---

### 3.2 필요한 커스텀 블록 (15개)

#### Phase 1: 필수 블록 (5개)
1. **Header** - 고정 헤더, 드롭다운 메뉴
2. **Footer** - 다단 레이아웃, SNS 링크
3. **Hero Section** - 비디오 배경, Gradient 텍스트
4. **Feature Card** - 3열 그리드, Hover 애니메이션
5. **CTA Section** - Gradient 배경, Glow 효과

#### Phase 2: 주요 블록 (5개)
6. **Page Hero** - 내부 페이지용 Hero
7. **Two Column Layout** - 좌우 2열 레이아웃
8. **Portfolio Card** - 2x2 그리드, 이미지 확대
9. **Testimonial Card** - 3열, 별점, 후기
10. **Contact Form** - 입력 폼, 유효성 검사

#### Phase 3: 추가 블록 (5개)
11. **Solutions Tabs** - 탭 전환, 콘텐츠 슬라이드
12. **Trust Logos** - 로고 그리드, 그레이스케일
13. **Stats Counter** - 숫자 카운팅 애니메이션
14. **Timeline** - 수직 타임라인, 스크롤 애니메이션
15. **Download List** - 파일 다운로드 리스트

---

## 4. 레이아웃 시스템

### 4.1 Container

```css
.container {
  max-width: 1280px;
  margin: 0 auto;
  padding: 0 80px;
}

/* Responsive */
@media (max-width: 1199px) {
  .container { padding: 0 48px; }
}

@media (max-width: 767px) {
  .container { padding: 0 24px; }
}
```

---

### 4.2 Section Spacing

```css
/* Presets */
.section-sm { padding: 60px 0; }
.section-md { padding: 80px 0; }
.section-lg { padding: 120px 0; }
.section-xl { padding: 160px 0; }

/* Mobile */
@media (max-width: 767px) {
  .section-sm { padding: 40px 0; }
  .section-md { padding: 60px 0; }
  .section-lg { padding: 80px 0; }
  .section-xl { padding: 100px 0; }
}
```

---

### 4.3 Grid System

```css
/* 2 Column */
.grid-2 {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 32px;
}

/* 3 Column */
.grid-3 {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 32px;
}

/* 4 Column */
.grid-4 {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 32px;
}

/* Responsive */
@media (max-width: 991px) {
  .grid-3 { grid-template-columns: repeat(2, 1fr); }
  .grid-4 { grid-template-columns: repeat(2, 1fr); }
}

@media (max-width: 767px) {
  .grid-2,
  .grid-3,
  .grid-4 {
    grid-template-columns: 1fr;
    gap: 16px;
  }
}
```

---

## 5. 커스텀 블록 개발 원칙

### 5.1 완전한 커스터마이징

**모든 요소가 설정 패널에서 수정 가능해야 함**:

- ✅ 모든 텍스트 (제목, 부제목, 버튼 텍스트 등)
- ✅ 모든 색상 (배경, 텍스트, 버튼 등)
- ✅ 모든 이미지 (배경, 로고, 아이콘 등)
- ✅ 모든 링크
- ✅ 모든 크기 (폰트, 여백, 높이 등)
- ✅ 표시/숨김 옵션

---

### 5.2 비개발자 친화적

**설정 패널 구성 원칙**:

1. **TITLE로 그룹핑**
   ```javascript
   {
     "type": "TITLE",
     "label": "📝 콘텐츠 설정"
   }
   ```

2. **명확한 라벨**
   - ❌ "Text 1"
   - ✅ "제목"

3. **도움말 제공**
   ```javascript
   {
     "type": "TEXT",
     "label": "제목",
     "placeholder": "예: AI가 만드는 신뢰할 수 있는 미래",
     "help": "메인 제목을 입력하세요 (권장: 30자 이내)"
   }
   ```

4. **2026 트렌드 기본값**
   ```javascript
   {
     "type": "RANGE",
     "id": "titleFontSize",
     "default": 48  // 2026 트렌드: 큰 폰트
   }
   ```

---

### 5.3 마우스 인터랙션

**각 페이지마다 1-2개 과감한 인터랙션 구현**:

#### Hover 효과
```css
.card {
  transition: all 0.3s ease;
}

.card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-lg);
}
```

#### Scroll 트리거
```javascript
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('is-visible');
    }
  });
}, { threshold: 0.1 });

document.querySelectorAll('.fade-in-on-scroll').forEach(el => {
  observer.observe(el);
});
```

#### 숫자 카운팅
```javascript
function animateCounter(element, target, duration) {
  let start = 0;
  const increment = target / (duration / 16);
  
  const updateCounter = () => {
    start += increment;
    if (start < target) {
      element.textContent = Math.floor(start);
      requestAnimationFrame(updateCounter);
    } else {
      element.textContent = target;
    }
  };
  
  updateCounter();
}
```

---

### 5.4 고품질 이미지

**이미지 소스**:
- ✅ Unsplash (https://unsplash.com)
- ✅ Pexels (https://pexels.com)
- ❌ 저작권 있는 이미지 사용 금지

**권장 해상도**:
- 배경 이미지: 1920x1080px 이상
- 카드 이미지: 800x600px 이상
- 썸네일: 400x300px 이상

**최적화**:
- WebP 형식 권장
- 파일 크기 500KB 이하

---

## 6. 설정 패널 구성

### 6.1 완전한 템플릿

```javascript
{
  "settings": [
    // ==================
    // 1. 콘텐츠 설정
    // ==================
    {
      "type": "TITLE",
      "label": "📝 콘텐츠 설정"
    },
    {
      "type": "TEXT",
      "id": "title",
      "label": "제목",
      "default": "AI가 만드는 신뢰할 수 있는 미래",
      "placeholder": "제목을 입력하세요"
    },
    {
      "type": "TEXTAREA",
      "id": "subtitle",
      "label": "부제목",
      "default": "금융, 제조, 의료, 소매 모든 산업에서 검증된 AI 솔루션",
      "rows": 3
    },
    {
      "type": "CHECKBOX",
      "id": "showButton",
      "label": "버튼 표시",
      "default": true
    },
    {
      "type": "TEXT",
      "id": "buttonText",
      "label": "버튼 텍스트",
      "default": "도입 신청하기",
      "isVisible": "{{showButton}}"
    },
    {
      "type": "LINK",
      "id": "buttonLink",
      "label": "버튼 링크",
      "default": "/demo",
      "isVisible": "{{showButton}}"
    },
    
    // ==================
    // 2. 타이포그래피
    // ==================
    {
      "type": "TITLE",
      "label": "✍️ 타이포그래피"
    },
    {
      "type": "RANGE",
      "id": "titleFontSize",
      "label": "제목 크기 (px)",
      "min": 24,
      "max": 72,
      "step": 2,
      "default": 48,
      "unit": "px"
    },
    {
      "type": "RANGE",
      "id": "titleFontWeight",
      "label": "제목 굵기",
      "min": 400,
      "max": 900,
      "step": 100,
      "default": 700
    },
    {
      "type": "RANGE",
      "id": "subtitleFontSize",
      "label": "부제목 크기 (px)",
      "min": 14,
      "max": 24,
      "step": 1,
      "default": 18,
      "unit": "px"
    },
    
    // ==================
    // 3. 색상
    // ==================
    {
      "type": "TITLE",
      "label": "🎨 색상"
    },
    {
      "type": "COLOR_PICKER",
      "id": "backgroundColor",
      "label": "배경색",
      "default": "#0055FF"
    },
    {
      "type": "COLOR_PICKER",
      "id": "titleColor",
      "label": "제목 색상",
      "default": "#FFFFFF"
    },
    {
      "type": "COLOR_PICKER",
      "id": "subtitleColor",
      "label": "부제목 색상",
      "default": "rgba(255, 255, 255, 0.9)"
    },
    {
      "type": "COLOR_PICKER",
      "id": "buttonBackgroundColor",
      "label": "버튼 배경색",
      "default": "#FFFFFF"
    },
    {
      "type": "COLOR_PICKER",
      "id": "buttonTextColor",
      "label": "버튼 텍스트 색상",
      "default": "#0055FF"
    },
    
    // ==================
    // 4. 레이아웃
    // ==================
    {
      "type": "TITLE",
      "label": "📐 레이아웃"
    },
    {
      "type": "RANGE",
      "id": "sectionPaddingTop",
      "label": "상단 여백 (px)",
      "min": 0,
      "max": 200,
      "step": 10,
      "default": 120,
      "unit": "px"
    },
    {
      "type": "RANGE",
      "id": "sectionPaddingBottom",
      "label": "하단 여백 (px)",
      "min": 0,
      "max": 200,
      "step": 10,
      "default": 120,
      "unit": "px"
    },
    {
      "type": "SELECT",
      "id": "contentAlignment",
      "label": "콘텐츠 정렬",
      "options": [
        { "value": "left", "label": "왼쪽" },
        { "value": "center", "label": "중앙" },
        { "value": "right", "label": "오른쪽" }
      ],
      "default": "center"
    },
    {
      "type": "RANGE",
      "id": "contentMaxWidth",
      "label": "콘텐츠 최대 너비 (px)",
      "min": 400,
      "max": 1280,
      "step": 40,
      "default": 800,
      "unit": "px"
    },
    
    // ==================
    // 5. 스타일
    // ==================
    {
      "type": "TITLE",
      "label": "💅 스타일"
    },
    {
      "type": "RANGE",
      "id": "borderRadius",
      "label": "모서리 둥글기 (px)",
      "min": 0,
      "max": 32,
      "step": 2,
      "default": 12,
      "unit": "px"
    },
    {
      "type": "CHECKBOX",
      "id": "showShadow",
      "label": "그림자 표시",
      "default": true
    },
    {
      "type": "SELECT",
      "id": "shadowSize",
      "label": "그림자 크기",
      "options": [
        { "value": "sm", "label": "작게" },
        { "value": "md", "label": "중간" },
        { "value": "lg", "label": "크게" }
      ],
      "default": "md",
      "isVisible": "{{showShadow}}"
    },
    
    // ==================
    // 6. 애니메이션
    // ==================
    {
      "type": "TITLE",
      "label": "✨ 애니메이션"
    },
    {
      "type": "CHECKBOX",
      "id": "enableHoverEffect",
      "label": "Hover 효과 활성화",
      "default": true
    },
    {
      "type": "CHECKBOX",
      "id": "enableScrollAnimation",
      "label": "스크롤 애니메이션 활성화",
      "default": true
    },
    {
      "type": "RANGE",
      "id": "animationDuration",
      "label": "애니메이션 속도 (초)",
      "min": 0.1,
      "max": 1.0,
      "step": 0.1,
      "default": 0.3,
      "unit": "s"
    }
  ]
}
```

---

## 7. 마우스 인터랙션

### 7.1 Hover 효과 예시

```css
/* Card Lift */
.card {
  transition: all var(--duration-normal) var(--ease);
}

.card:hover {
  transform: translateY(-8px);
  box-shadow: var(--shadow-lg);
}

/* Icon Rotation */
.icon {
  transition: transform var(--duration-fast) ease;
}

.card:hover .icon {
  transform: rotate(5deg) scale(1.1);
}

/* Color Change */
.link {
  color: var(--gray-700);
  transition: color var(--duration-fast) ease;
}

.link:hover {
  color: var(--primary-500);
}
```

---

### 7.2 Scroll 트리거 예시

```javascript
// Intersection Observer로 스크롤 애니메이션
const observerOptions = {
  threshold: 0.1,
  rootMargin: '0px 0px -50px 0px'
};

const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.classList.add('is-visible');
    }
  });
}, observerOptions);

// 모든 애니메이션 요소 관찰
document.querySelectorAll('.fade-in-on-scroll').forEach(el => {
  observer.observe(el);
});
```

```css
/* CSS 애니메이션 */
.fade-in-on-scroll {
  opacity: 0;
  transform: translateY(50px);
}

.fade-in-on-scroll.is-visible {
  animation: fade-in-up 0.8s ease forwards;
}

@keyframes fade-in-up {
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
```

---

### 7.3 숫자 카운팅 예시

```html
<template>
  <div class="stats">
    <div class="stat-item">
      <span class="stat-number" data-target="150">0</span>
      <span class="stat-label">고객사</span>
    </div>
  </div>
</template>
```

```javascript
function animateCounters() {
  const counters = document.querySelectorAll('.stat-number');
  
  counters.forEach(counter => {
    const target = parseInt(counter.getAttribute('data-target'));
    let current = 0;
    const increment = target / 100;
    const duration = 2000; // 2초
    const stepTime = duration / 100;
    
    const updateCounter = () => {
      current += increment;
      if (current < target) {
        counter.textContent = Math.floor(current);
        setTimeout(updateCounter, stepTime);
      } else {
        counter.textContent = target + '+';
      }
    };
    
    updateCounter();
  });
}

// Intersection Observer와 결합
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      animateCounters();
      observer.unobserve(entry.target); // 한 번만 실행
    }
  });
});

document.querySelectorAll('.stats').forEach(el => {
  observer.observe(el);
});
```

---

## 8. 페이지별 개발 가이드

### 8.1 HOME 페이지

**필요한 블록**:
1. Header
2. Hero Section
3. Feature Card (3개)
4. Solutions Tabs
5. Trust Logos
6. Portfolio Card (4개)
7. Testimonial Card (3개)
8. CTA Section
9. Footer

**인터랙션**:
- Hero: 비디오 배경 자동재생, Gradient 텍스트
- Feature Card: Hover 시 lift + 아이콘 회전
- Portfolio Card: 이미지 확대 + 색상 변화
- Stats: 숫자 카운팅 애니메이션

---

### 8.2 산업별 솔루션 페이지

**구조**:
1. Header
2. Page Hero (산업별 이미지)
3. Feature Card (3개 - 주요 과제)
4. Two Column Layout (솔루션 설명)
5. Stats Counter (성과 수치)
6. Portfolio Card (2개 - 사례)
7. CTA Section
8. Footer

**인터랙션**:
- Page Hero: 배경 이미지 패럴렉스
- Two Column: 교대 레이아웃 (좌/우)
- Stats: 스크롤 시 카운팅 시작

---

## 9. 품질 기준

### 9.1 기능 체크리스트

- [ ] 모든 텍스트 수정 가능
- [ ] 모든 이미지 교체 가능
- [ ] 모든 색상 변경 가능
- [ ] 모든 링크 수정 가능
- [ ] 표시/숨김 옵션 작동
- [ ] 반응형 디자인 완벽

---

### 9.2 설정 패널 체크리스트

- [ ] TITLE로 논리적 그룹핑
- [ ] 명확한 라벨 사용
- [ ] 도움말/플레이스홀더 제공
- [ ] 2026 트렌드 기본값
- [ ] isVisible 조건 활용

---

### 9.3 디자인 체크리스트

- [ ] 2026 트렌드 반영 (큰 폰트, 둥근 모서리, Glow)
- [ ] 색상 일관성
- [ ] 타이포그래피 일관성
- [ ] 간격 일관성
- [ ] 고품질 이미지 사용

---

### 9.4 인터랙션 체크리스트

- [ ] Hover 효과 구현 (lift, 색상 변화 등)
- [ ] 스크롤 애니메이션 구현
- [ ] 숫자 카운팅 (해당 시)
- [ ] 부드러운 전환 (0.3s ease)
- [ ] 애니메이션 과하지 않음

---

### 9.5 반응형 체크리스트

- [ ] 데스크톱 (1920px) 테스트
- [ ] 노트북 (1280px) 테스트
- [ ] 태블릿 (768px) 테스트
- [ ] 모바일 (375px) 테스트
- [ ] 터치 타겟 48px 이상 (모바일)

---

## 10. 부록

### 10.1 Feature Card 블록 완전한 예시

```html
<style>
  .feature-section {
    padding: {{sectionPaddingTop}}px 0 {{sectionPaddingBottom}}px;
    background: {{backgroundColor}};
  }
  
  .feature-container {
    max-width: 1280px;
    margin: 0 auto;
    padding: 0 80px;
  }
  
  .feature-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 32px;
  }
  
  .feature-card {
    padding: 40px;
    background: var(--white);
    border: 1px solid var(--gray-100);
    border-radius: {{borderRadius}}px;
    transition: all {{animationDuration}}s var(--ease);
    cursor: default;
  }
  
  {{#if enableHoverEffect}}
  .feature-card:hover {
    transform: translateY(-8px);
    box-shadow: var(--shadow-lg);
    border-color: var(--primary-500);
  }
  {{/if}}
  
  .feature-icon {
    width: 64px;
    height: 64px;
    margin-bottom: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    background: {{iconBackgroundColor}};
    border-radius: 12px;
    transition: all {{animationDuration}}s ease;
  }
  
  {{#if enableHoverEffect}}
  .feature-card:hover .feature-icon {
    transform: scale(1.1) rotate(5deg);
    background: {{iconHoverBackgroundColor}};
  }
  {{/if}}
  
  .feature-title {
    font-size: {{titleFontSize}}px;
    font-weight: {{titleFontWeight}};
    color: {{titleColor}};
    margin-bottom: 16px;
  }
  
  .feature-description {
    font-size: {{descriptionFontSize}}px;
    line-height: 1.6;
    color: {{descriptionColor}};
  }
  
  @media (max-width: 991px) {
    .feature-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (max-width: 767px) {
    .feature-container {
      padding: 0 24px;
    }
    
    .feature-grid {
      grid-template-columns: 1fr;
      gap: 16px;
    }
    
    .feature-card {
      padding: 32px 24px;
    }
  }
</style>

<template>
  <section class="feature-section {{#if enableScrollAnimation}}fade-in-on-scroll{{/if}}">
    <div class="feature-container">
      <div class="feature-grid">
        {{#each features}}
          <div class="feature-card">
            <div class="feature-icon">
              <svg width="48" height="48">
                {{{this.icon}}}
              </svg>
            </div>
            <h3 class="feature-title">{{this.title}}</h3>
            <p class="feature-description">{{this.description}}</p>
          </div>
        {{/each}}
      </div>
    </div>
  </section>
</template>

<script>
  {{#if enableScrollAnimation}}
  // Scroll Animation
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
      }
    });
  }, { threshold: 0.1 });
  
  document.querySelectorAll('.fade-in-on-scroll').forEach(el => {
    observer.observe(el);
  });
  {{/if}}
</script>

<data>
{
  "sectionPaddingTop": 120,
  "sectionPaddingBottom": 120,
  "backgroundColor": "#FFFFFF",
  "borderRadius": 16,
  "animationDuration": 0.3,
  "enableHoverEffect": true,
  "enableScrollAnimation": true,
  "iconBackgroundColor": "#E6F0FF",
  "iconHoverBackgroundColor": "#0055FF",
  "titleFontSize": 22,
  "titleFontWeight": 600,
  "titleColor": "#212529",
  "descriptionFontSize": 16,
  "descriptionColor": "#6C757D",
  "features": [
    {
      "icon": "<path d='...'/>",
      "title": "빠른 도입",
      "description": "평균 6주 만에 운영 시작. 레거시 시스템과의 원활한 통합."
    },
    {
      "icon": "<path d='...'/>",
      "title": "검증된 신뢰성",
      "description": "금융권 99.8% 정확도, 의료 분야 FDA 인증 획득."
    },
    {
      "icon": "<path d='...'/>",
      "title": "완전한 보안",
      "description": "ISO 27001, SOC2 Type II 인증. 데이터 주권 보장."
    }
  ]
}
</data>
```

---

## ✅ 최종 점검

**제출 전 필수 확인**:

- [ ] 커스텀 블록 10개 이상 완성
- [ ] 신규 제작 블록 5개 포함
- [ ] 모든 블록에 설정 패널 완비
- [ ] 2026 트렌드 반영
- [ ] 마우스 인터랙션 풍부
- [ ] 반응형 디자인 완벽
- [ ] 고품질 무료 이미지 사용
- [ ] 통이미지 없음
- [ ] 저작권 문제 없음
- [ ] 브라우저 호환성 테스트 완료


